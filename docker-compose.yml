version: "3.9"

services:
  parser:
    build: .
    restart: unless-stopped
    volumes:
      - ./config.toml:/app/config.toml
      - ./data.json:/app/data.json
      - ./sessions:/app
    working_dir: /app
